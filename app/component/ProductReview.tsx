export const ProductReview = async () => {
    // Introduce a delay of 6 seconds (6000ms)
    await new Promise(resolve => setTimeout(resolve, 6000));

    return (
        <div>Product review</div>
    );
};
